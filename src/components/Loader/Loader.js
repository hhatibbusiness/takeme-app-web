import React from 'react';
import './Loader.scss';

const Loader = () => {
    return (
        <div className={'Loader'}>
            <div className="Loader__spinner"></div>
        </div>
    );
};

export default Loader;